PORT=3000
NODE_ENV=test

# Set by Testcontainers during Jest global setup. This fallback keeps local/CI config validation predictable.
DATABASE_URL=postgresql://test:test@localhost:5432/nestjs_boilerplate_test

SALT_ROUND=3
JWT_SECRET=test-secret
JWT_ISSUER=test-issuer
JWT_AUDIENCE=test-audience
ACCESS_TOKEN_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d

LOGGER_LEVEL=silent
CORS_ALLOWED_ORIGINS=http://localhost:3000
